name: azureml-training
channels:
  - anaconda
  - conda-forge
dependencies:
  - python=3.8
  - pip:
      - 'azureml-dataset-runtime[pandas,fuse]'
      - azureml-defaults==1.36.0
      - numpy
      - scikit-learn